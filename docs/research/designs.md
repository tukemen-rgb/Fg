# 技術設計（役割②の出力）

役割②が proposals.md の最新を実行可能な形に落とす。新しいものを上に。
役割③はここの最新の「未実装」を実装し、済んだら「実装済み <hash>」と印を付ける。

必須の節: 変更対象ファイル / 構成 / 手順 / **リスク（財務・法務・信用/ブランド。
経営ゲート・意思決定原則・既存文書と矛盾しないこと。対外発信と金額の決定は
人に残す）**

---

## 2026-08-09 21:35 JST 声かけの「経路」候補を一覧にする — 状態: 実装済み `3532b7b`

提案: proposals.md 2026-08-09 21:15（実測で投稿 0 件・外部登録者 0 人が確定。
事例 22・23・24・15）。恒常規則に従い**既存ファイルの磨き込みのみ**。
**要判断の経路を扱う**ので、実施・推奨と読まれない書き方を他の周より厳しく見る。

### 変更対象ファイル

1. `docs/research/interviews.md` — 1 か所（「声かけの記録」節に表を 1 つ追加）
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

**既存の記録表（日付・相手・経路・結果）と台帳には触れない。** データ行は 0 のまま。

### 構成（追加する 1 か所。挿入位置は「声かけの記録」の箇条書きの直後・
既存の記録表の直前）

挿入するのは次の見出し 1 つ・本文 3 行・表 1 つ・注記 3 行。**下記をそのまま書く**。

```
### 経路の候補（選べる一覧。実施の推奨ではない）

**どの経路で当たったかを後から比べられるように、経路名を先に固定する。**
下の記録表の「経路」欄には、この表の経路名をそのまま書く。
**要判断**の行は、人が決めるまで役割②以降の設計・実装に流さない。

| 経路 | いまの状態 | この経路を使う前に人が決めること |
| --- | --- | --- |
| GAMEYARD 公式アカウント | 使える | なし（2026-08-09 に社長が開始と回答） |
| 制作者への個別連絡 | 使える | なし（上の「断られた相手に重ねて声をかけない」が適用される） |
| ジャム開催 | **要判断** | 順位を付けない方針（`docs/pr/press-kit.md` の「広告・スポンサーの扱い」）のもとで、何を動機にするか |
| 第三者に依頼する広報 | **要判断** | 表示の責任は依頼した事業者にある（case-studies.md の 15）。個別の適法性は人が判断する |
| 広告出稿 | **要判断** | 金額・契約・支払いの決定（autonomous-loop.md「人が決めるまで変えない一覧」） |

- 経路を足すのは役割①。**出典のある事実としてのみ足す**（推測で足さない）
- 「いまの状態」は**使える / 要判断**の 2 値だけ。進捗や見通しは書かない
- 条件の中身はここに再掲しない（参照先を 1 つだけ書く。二重管理を作らない）
```

### 手順（役割③）

1. `docs/research/interviews.md` を開き、「声かけの記録（0 件）」節の
   箇条書き最終行（`- 2026-08-09: 社長が…追記する`）の**直後**、
   既存の `| 日付 | 相手（ハンドル） | 経路 | 結果 |` の**直前**に、
   上のブロックをそのまま挿入する
2. 既存の記録表・その下の「結果は…」の 2 行・台帳が**1 文字も変わっていない**ことを
   `git diff` で確認する（削除行が 0 であること）
3. 禁止語の混入を確認: `git diff -U0 | grep '^+' | grep -n "べき\|おすすめ\|推奨\|望ましい\|有望\|見込み\|予定\|だろう\|違法\|問題ない"` が
   **空**であること（「推奨ではない」の 1 か所のみ許容。件数で判断せず、
   出た行を目で確かめる）
4. 参照先の実在確認: `docs/pr/press-kit.md` に「広告・スポンサーの扱い」節が
   あること、`case-studies.md` に `^## 15\. ` があること、
   `autonomous-loop.md` に「人が決めるまで変えない一覧」があることを
   **区切りつきの grep** で確かめる（環境メモの部分一致に注意）
5. `designs.md` の本節を「実装済み <hash>」に、`proposals.md` の
   2026-08-09 21:15 を「実装済み」に更新
6. `worklog.md` に 3 行以内で追記し、commit・push（作業ブランチのみ）

### リスク

| 観点 | リスク | 抑え方 |
| --- | --- | --- |
| **財務** | 広告出稿・ジャムの賞金は金額の決定を含む。表に金額・予算感が入ると、決めていないものを決めたように読まれる | 表に**数字を一切書かない**。広告出稿行の「決めること」は金額・契約・支払いの決定そのものを指し、参照先を示すだけにする |
| **法務** | 第三者依頼の行が、適法性の判断に読まれる | 事実（表示の責任は依頼した事業者にある）と**判断は人**だけを書く。「違法」「問題ない」の語を使わない（事例 15 で確立した書き方を踏襲） |
| **信用/ブランド** | 一覧に並べただけで「やる候補」と読まれ、順位を付けない方針や規制対応が緩んだように見える | 見出しに**「実施の推奨ではない」**を入れる。要判断は 3 行とも**要判断のまま**残し、解除条件を書かない（解除は人の判断） |
| **既存文書との整合** | KPI・経営ゲート・事業の順序に触れない | この表は**経路の名前の一覧**であり、目標値・指標・順序を一切変更しない。README・STRATEGY・ROADMAP_90D・KPI は無変更 |
| **二重管理** | press-kit のスポンサー条件、autonomous-loop の要判断一覧、事例 15 を再掲すると、片方だけ古くなる | 各行の参照先は**1 つだけ**。条件の中身は書かず、参照先を読ませる |
| **範囲外** | 対外発信・実施は人の領域 | この設計は**文書の追加のみ**。声をかける主体は人（既存の「声をかけるのは人」を維持） |

### 実装しないこと

- 記録表へのデータ行の追加（実施が無いのに行を作らない）
- 経路ごとの目標件数・優先順位（順位付けは経営の判断）
- 「ジャムをやる場合の設計」（要判断のため②以降に流さない）

## 2026-08-09 20:25 JST 本番サーバーの実測値を経営記録に入れる — 状態: 実装済み `6666b19`

提案: proposals.md 2026-08-09 20:15（社長が本番で実測。事例 12・21）。
恒常規則に従い**既存ファイルの磨き込みのみ**。**この周で初めて実数を書く**ので、
数字の扱いを他の周より厳しく見る。

### 変更対象ファイル

1. `docs/finance/monthly-record.md` — 4 か所（KPI 表 1 行・経営ゲート判定 1 行・
   特記事項に実測の記録・A 指標の集計手順に本番の場所と実測コマンド）
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（4 か所。実測は 2026-08-09 のスナップショット）

**(1) KPI 表 35 行目**（供給・新規投稿作品数/週）

- 「今月実績」: `未計測` → **`0 件（2026-08-09 実測）`**
- 「計測手段」: **`A: 本番サーバーで実測できる（下の集計手順を参照）`**

**(2) 経営ゲート判定 50 行目**

- `| 新規投稿作品 10本 | 判定不能（未計測） |` →
  `| 新規投稿作品 10本 | **未達（実測 0 件・2026-08-09）** |`

**(3) 特記事項に小見出しを 1 つ追加**（「B 指標を実装する前に決めること」の**前**）

```
#### 2026-08-09 本番サーバーの実測

社長が本番サーバーで実測した値。**これが 90 日計画の出発点の数字**。

| 実測項目 | 値 | 確認方法 |
| --- | --- | --- |
| 投稿作品数 | **0 件** | `find /var/lib/gameyard/store/games -name '*.json' | wc -l` |
| 登録アカウント | **1 件（うち社内 1・外部 0）** | `find .../store/accounts -name '*.json' | wc -l`。1 件は社長本人（2026-08-09 の回答） |
| 審査待ち | **0 件** | `find /var/lib/gameyard/pending -type f | wc -l` |
| サービス稼働 | **api / game-host / web の 3 つとも active running** | `systemctl`。2026-08-06 から継続 |

サービスは動いており、投稿と外部登録が 0。**技術の不具合ではなく、
到達していないという状態**として記録する。
```

**(4) A 指標の集計手順に「本番での数え方」を追記**（節の末尾）

```
### 本番での数え方（2026-08-09 に実地で確認）

投稿レコードの実体は **`/var/lib/gameyard/store/games/*.json`**
（`deploy/gameyard.env.example` の `STORE_DIR=/var/lib/gameyard/store`）。
サーバーにログインして件数を数える:

    find /var/lib/gameyard/store/games -name '*.json' | wc -l

週ごとに分けるときは各 JSON の `createdAt` を上の 1〜4 の定義で割り当てる。
export を待たなくても、この場で数えられる。
```

**変えないもの**: KPI の指標名・初期目標、経営ゲートの条件文、B 指標の
定義の表、収支表、他の特記事項

### 手順

1. 上の 4 か所を編集する
2. 読み合わせ:
   - (a) **書いてよい数字は実測の 4 つだけ**。週次の内訳・率（投稿完了率など）は
     **計算しない**（分母が 0 で意味を成さない指標がある）
   - (b) 登録アカウントは**必ず内訳つき**（社内 1・外部 0）。「1 人」とだけ書かない
   - (c) 経営ゲートの他 5 条件は**触らない**（判定不能のまま。実測したのは 1 つだけ）
   - (d) KPI の指標名・初期目標が README・KPI.md と一致したままであること
3. git diff で**削除が 2 行だけ**（KPI 表 1 行・ゲート判定 1 行の置き換え）で
   あることを確認する
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用（最大）**: 初めて実数を入れる周なので、**書きすぎ**が最大リスク。
  実測していない指標に 0 を波及させない（例: 「週次アクティブプレイヤー 0」は
  **計測手段が無いので書けない**。0 と未計測は違う）
- **信用**: 「登録 1 件」を成果に見せない。**外部 0** を必ず併記する
- **運用**: 実測はスナップショットなので**日付を必ず添える**。次に数えた
  ときに比較できる形にする
- **財務/法務/対外発信**: なし（内部記録。press-kit には反映しない
  — 対外資料に「投稿 0 件」を載せるかは人の判断で、今回は触らない）
- **整合**: KPI.md の指標・目標値は不変。README の経営ゲート条件も不変。
  第 4 周の A/B 分類（A は 1 指標）とも矛盾しない（A の実測手段が
  「人の export」から「サーバーで直接」に具体化しただけ）

## 2026-08-09 19:25 JST 恒常規則 4 本目を規則文書へ反映 — 状態: 実装済み a7daba3

提案: proposals.md 2026-08-09 19:15（⑤裁定 18:50・事例 11）。
恒常規則に従い**既存ファイルの磨き込みのみ**。**転記であり、緩和も追加も
しない。**

### 変更対象ファイル

1. `docs/autonomous-loop.md` — 「### 恒常規則（⑤の裁定で追加。緩和はしない）」
   の表に 1 行追加（現在 3 行 → 4 行）
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（追加する 1 行。既存 3 行の末尾に置く）

| 規則 | 内容 | 解除条件 | 出典 |
| --- | --- | --- | --- |
| 出典が無い周は点検にあてる | 事例の出典が取れない周は、手持ちの文書の点検にあてる（放置でも推測でもなく、点検 → 穴が有れば提案・無ければその旨を残す） | なし（常時） | mediation.md 2026-08-09 18:50 |

**変えないもの**: 既存 3 行の文言・順序、表の直後の 2 行
（「この表は⑤の裁定の転記…」以下）、他の節すべて

### 手順

1. 表の末尾（「1 周を軽くする」の行の後）に上の 1 行を追加する
2. 読み合わせ: (a) 内容が mediation.md 18:50 の記述と一致すること、
   (b) 既存 3 行が一字も変わっていないこと、(c) 表の行数が 4 になること
   （ヘッダと区切りを除く）
3. git diff が**追加 1 行のみ**（削除 0）であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 転記のずれ（意味が変わる要約）が最大リスク。出典の日時を
  書き、内容を足さない・削らない。**規則を緩める方向の書き換えはしない**
- **運用**: ⑤が申し送った「点検は当面繰り返さない」は**規則ではなく
  その時点の申し送り**なので、規則の表には書かない
  （mediation.md 18:50 に残っている）。表に書くと恒久の制限になり、
  必要なときの点検まで止まる
- **財務/法務/対外発信**: なし（ループ運用の記述のみ）
- **整合**: README・STRATEGY・ROADMAP_90D・KPI に触れない。
  恒常規則（受け皿新設停止）にも適合（新規ファイルを作らない）

## 2026-08-09 18:25 JST 器の点検結果を記録 — 状態: 実装済み 5ff76b7

提案: proposals.md 2026-08-09 18:15（点検結果は同提案に表で記載）。
恒常規則に従い**既存ファイルの磨き込みのみ**。**器は作らない**（条件待ち）。

### 変更対象ファイル

1. `docs/finance/monthly-record.md` — 「4. 特記事項」の
   「2026-08-10 週の広報発信・営業の起点日」の小見出しの**後ろ**に
   小見出しを 1 つ追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（小見出し 1 つ＋3 文まで）

```
#### CreatorYard 送客の記録は条件待ち（2026-08-09 の点検）

README の事業の順序 4 番「CreatorYard へ制作者を送客し、Creator Partner を
検証する」に対応する記録の器は、Fg 側にまだ無い。**creater-yard は
2026-08-08 時点で雛形段階**（棚卸しで確認。app に layout/page のみで
掲載データの仕組みが無い）で、送客先の実体が無いため今は作らない。
creater-yard に掲載の仕組みができた時点で、送客と Creator Partner の
検証の記録を作る。
```

**変えないもの**: KPI 表・経営ゲート判定・A 指標の集計手順・B 指標の
定義の表・既存の特記事項。**実数や見通し（いつできるか）は書かない**

### 手順

1. 上の小見出しを該当箇所の後ろに追加する
2. 読み合わせ: (a) 「雛形段階」の根拠が第 4 周の棚卸し（2026-08-08 確認）と
   一致すること、(b) **いつ実体ができるかの予想を書いていない**こと、
   (c) creater-yard 側への指示・催促になっていないこと（分担 0d4777d）
3. git diff が**追加のみ**（削除 0）であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 他部門の進み具合を Fg が評価する形になりやすい。**事実（雛形
  段階・確認日）と Fg 側の対応（今は作らない）だけを書き、「遅れている」
  「早く作るべき」に類する語を使わない**（第 10 周の裁定: Fg は催促しない）
- **運用**: 条件が満たされたことに誰も気づかない恐れがある。①が
  creater-yard を参照した周に気づける形にするため、**点検の表は
  proposals.md（2026-08-09 18:15）に残したまま**にし、monthly-record 側は
  1 か所に短く書く（二重管理にしない）
- **財務/法務/対外発信**: なし（内部記録）
- **整合**: README の事業の順序 4 番を**参照するだけ**で、順序・条件は
  変えない。KPI「CreatorYard掲載制作者 5」の扱い（B 指標・未計測）とも
  矛盾しない

## 2026-08-09 17:25 JST 声かけの記録欄を足す — 状態: 実装済み 7905be7

提案: proposals.md 2026-08-09 17:15（事例 22・18）。恒常規則に従い
**既存ファイルの磨き込みのみ**。実施・送信は人。

### 変更対象ファイル

1. `docs/research/interviews.md` — 「## 台帳」の**前**に「## 声かけの記録」
   の節を追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（新しい節 1 つ。行は空のまま）

見出しは「## 声かけの記録（0 件）」。直下に 3 行:

- **インタビューに至らなかった声かけもここに残す。** 台帳（下）は
  「聞けた」の記録で、この表は「声をかけた」の記録
- 相手は**ハンドル名のみ**（本人が公開している範囲）。断られた相手に
  重ねて声をかけない
- **声をかけるのは人**。この表は結果を書き留める場所

表のヘッダ（データ行は 0 行。ヘッダと区切り行だけ置く）:

| 日付 | 相手（ハンドル） | 経路 | 結果 |
| --- | --- | --- | --- |

結果の書き方を 1 行: 「結果は
**返信なし / 断り / 話を聞けた / 投稿に至った** のいずれか。
『話を聞けた』は台帳の番号（例: 台帳 3）も書く」

**変えないもの**: 台帳の見出し・データ行 10 行・第 1 巡/第 2 巡の区切り・
質問票・運用規則・記録テンプレート

### 手順

1. 「## 台帳（経営ゲート進捗: 0 / 10 件）」の直前に上の節を追加する
2. 読み合わせ: (a) 台帳のデータ行が **1〜10 各 1 回で不変**
   （区切りつきで数える。環境メモ 2026-08-09）、(b) 声かけの表に
   **データ行が無い**こと（架空の実績を書かない）、(c) 「声をかけるのは人」
   が明記されていること
3. git diff が**追加のみ**（削除 0）であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 架空の声かけ実績を AI が埋めるのが最大リスク。**行は 0 のまま**。
  「0 件」を見出しに書き、実績が無いことを隠さない
- **法務/プライバシー**: 断られた相手の記録が残る。**ハンドル名のみ**とし、
  本名・連絡先・断りの理由の詳細は書かない（git 履歴は消せない）。
  既存の運用規則（相手はハンドル名のみ）と同じ扱い
- **対外発信**: 声をかける行為そのものは**人**。この表は記録の器で、
  依頼文の作成・送信を促すものではない（「対外発信は下書きまで」の規則）
- **財務**: なし（謝礼は決めない）
- **整合**: README の事業の順序 2 番目（最初の投稿者とプレイヤーを手動でも
  獲得する）に対応する記録。経営ゲートの件数・条件は変えない

## 2026-08-09 16:25 JST B 指標の「定義の空欄」を置く — 状態: 実装済み b2441da

提案: proposals.md 2026-08-09 16:15（事例 21・19）。恒常規則に従い
**既存ファイルの磨き込みのみ**。**値は書かない**（決めるのは site 側・人）。

### 変更対象ファイル

1. `docs/finance/monthly-record.md` — 第 15 周のボット除外の段落
   （「方式と除外の決定は site 側・人。」で終わる段落）の**直後**に
   小見出しと表を 1 つ追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（小見出し 1 つ＋4 行の表。値はすべて未定）

```
#### B 指標を実装する前に決めること（値は site 側と人が決める）

| 決めること | いま | 備考 |
| --- | --- | --- |
| 訪問（セッション）の区切り | （未定） | 無操作が何分続いたら別の訪問とするか。他社の例: 30 分（case-studies.md の 21） |
| 週の区切り | **JST 月曜 00:00 始まり** | A 指標の集計手順で決定済み。B もこれに合わせると比べられる |
| 同一人物の数え方 | （未定） | 日をまたぐ再訪・別端末を同じ人と見るか（事例 21 では別に数える） |
| ボットの除き方 | （未定） | 上の段落のとおり、方式を選ぶ前に決める |

4 つが埋まるまで、B 指標の実数は monthly-record に書かない
（何を数えた数字か分からない状態で記録すると、後から直せない）。
```

**変えないもの**: KPI 表・経営ゲート判定・A 指標の集計手順・
既存の棚卸しとボット除外の段落。**実数は書かない**

### 手順

1. 上の小見出しと表を該当箇所の直後に追加する
2. 読み合わせ: (a) 「週の区切り」の値が A 指標の集計手順の記述
   （JST 月曜 00:00 始まり）と**一致**すること（機械照合）、
   (b) 他の 3 行が「（未定）」のままで、推奨値を書いていないこと、
   (c) 30 分が**他社の例としての引用**と読めること（自社の決定と読めない）
3. git diff が**追加のみ**（削除 0）であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 「（未定）」の欄に AI が値を入れるのが最大リスク。他社の例は
  **備考欄に出典つきで置き、決定欄には書かない**。30 分を SIDRA の
  決定と誤読させない
- **越境**: 実装方針を決める文書ではない。**推奨も指示も書かない**
  （分担 0d4777d）。site への push もしない
- **財務/法務**: なし
- **対外発信**: なし（内部記録）
- **整合**: KPI.md の指標・目標値は不変。A 指標の集計手順と矛盾しない
  （むしろ週の区切りを共有する）。第 15 周のボット除外の段落を
  重複させず、表の 1 行から参照する

## 2026-08-09 15:25 JST インタビューの学びを「効く成果」で並べる — 状態: 実装済み c4f2316

提案: proposals.md 2026-08-09 15:15（事例 20・18）。恒常規則に従い
**既存ファイルの磨き込みのみ**。空欄を作るだけで、中身は実施後に書く。

### 変更対象ファイル

1. `docs/research/interviews.md` — 記録テンプレートに 1 行追加、
   第 1 巡と第 2 巡のあいだに「第 1 巡のまとめ」の空欄を追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（変更 2 点。選択肢は README の 6 条件をそのまま使う）

1. 記録テンプレート（```で囲まれた部分）の「- 学び: <1 行>」の**前**に
   1 行追加:
   ```
   - 効く成果: <下の 6 つから 1 つ / 該当なし>
   ```
   テンプレートの直後に選択肢を箇条書きで置く（**README の文言のまま**）:
   投稿完了率 50%以上 / 新規投稿作品 10本 / 週次アクティブプレイヤー 100人 /
   投稿作品の累計プレイ 300回 / 制作者インタビュー 10件 /
   再訪率または7日継続率を計測可能にする
   — 併せて 1 行: 「**迷ったら「該当なし」。無理に当てはめない**
   （当てはめると、効かない改善が優先されることがある）」
2. 「### 第 1 巡（1〜5 件）」の表の**後**、第 2 巡の見出しの**前**に:
   ```
   ### 第 1 巡のまとめ（5 件終わってから書く）

   困りごとを「効く成果」ごとに並べ、上から順に site 側への改善提案にする。
   ここが埋まるまで 6 件目に進まない。

   （まだ無い）
   ```

**変えないもの**: 質問票・運用規則・台帳のデータ行・件数（10 件）・
経営ゲートの条件の文言

### 手順

1. 上の 2 点を編集する。選択肢 6 つは README から**転記**し、言い換えない
2. 読み合わせ: (a) 6 条件が README と一字一句一致すること（機械照合）、
   (b) 台帳のデータ行が 1〜10 の各 1 回で不変（区切りつきで数える。
   環境メモ 2026-08-09）、(c) まとめ欄が**空欄のまま**であること
   （中身を先に書かない）
3. git diff が**追加のみ**（削除 0）であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 聞いた話を成果に**無理に当てはめる**のが最大リスク。
  「該当なし」を選べるようにし、その旨を明記する。当てはめを強いると
  効かない改善が優先され、インタビューの意味が失われる
- **運用**: まとめ欄に今 AI が中身を書くと、実施前の推測が既成事実に
  なる。**空欄のまま置き、中身は実施後に人と①が書く**
- **財務/法務**: なし（謝礼・個人情報の扱いは既存の運用規則のまま）
- **対外発信**: なし。実施・連絡は人
- **整合**: README の経営ゲート 6 条件を**参照するだけ**で、条件・件数・
  順序は変えない。KPI.md・ROADMAP_90D にも触れない

## 2026-08-09 14:25 JST B 指標に「ボットの除き方を先に決める」注記 — 状態: 実装済み 2e0ad74

提案: proposals.md 2026-08-09 14:15（事例 19・7・8）。恒常規則に従い
**既存ファイルの磨き込みのみ**。方式の推奨はしない（決めるのは site 側・人）。

### 変更対象ファイル

1. `docs/finance/monthly-record.md` — 「4. 特記事項」の棚卸しの段落
   （「B を実装する場合、個人追跡なしの計測が実在する…導入判断は
   site 側・人。」で終わる段落）の**直後**に 1 段落追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（追加する 1 段落。3 文まで）

```
B を実装するときは、方式（JS 計測 / サーバーログ）を選ぶ前に
**ボットをどう除くか**を決める。決めずに数えると、収集カタログ 2 万件超を
持つ性質上クローラーが多く、数字が実態より大きく出る（実例: ログ解析の
ページビューが JS 計測の 18 倍。docs/research/case-studies.md の 19）。
方式と除外の決定は site 側・人。
```

**変えないもの**: KPI 表・経営ゲート判定・A 指標の集計手順・
既存の棚卸しの段落。**実数は書かない**

### 手順

1. 上の段落を該当箇所の直後に追加する
2. 読み合わせ: (a) 方式の推奨・優劣の断定を書いていないこと
   （「JS 計測のほうがよい」等を書かない。事例 19 は長所短所の両方を
   挙げている）、(b) 18 倍という数字が**事例 19 の実例の引用**として
   読める形になっていること（GAMEYARD の予測値と読めないこと）
3. git diff が**追加のみ**（削除 0）であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 「18 倍」を GAMEYARD の見込みと誤読されるのが最大リスク。
  **他社の実例として出典つきで書き、自社の予測は書かない**。
  「大きく出る恐れがある」までに留め、倍率の予想はしない
- **越境**: site 側の実装方針を決める文書ではない。**推奨も指示も
  書かない**（分担 0d4777d: Fg は経営記録、実装は site 側）。
  site への push もしない
- **財務/法務**: なし（ツールの費用・契約に触れない）
- **対外発信**: なし（内部記録）
- **整合**: KPI.md の指標・目標値は不変。README 経営ゲートにも触れない。
  第 4 周の棚卸し（A/B 分類）と矛盾しない補足として置く

## 2026-08-09 13:25 JST インタビュー台帳を 5 件 2 区切りにする — 状態: 実装済み 52077b2

提案: proposals.md 2026-08-09 13:15（事例 18・6）。恒常規則に従い
**既存ファイルの磨き込みのみ**。件数（10 件）と質問票の中身は変えない。

### 変更対象ファイル

1. `docs/research/interviews.md` — 台帳の見出しと区切り、質問票の見出し注記
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（変更は 3 点。表の行は 10 行のまま）

1. 台帳の見出し「## 台帳（経営ゲート進捗: 0 / 10 件）」は**そのまま**。
   直下に 1 行足す:
   「**5 件終わったら、そこまでの学びを反映してから 6 件目に進む**
   （反映先: site 側への改善提案・質問票 v2）。1 回 10 件より
   5 件 × 2 回のほうが改善が 2 度入る（case-studies.md の 18）」
2. 表を **2 つに割る**（1〜5 と 6〜10）。それぞれに小見出しを付ける:
   「### 第 1 巡（1〜5 件）」「### 第 2 巡（6〜10 件・第 1 巡の反映後）」。
   **各行の中身（実施日・相手・状態）は現状のまま**（全行「未実施」）
3. 質問票の見出し「## 質問票 v1」の直下に 1 行:
   「**v2 は第 1 巡（5 件）が終わってから作る。**途中で設問を変えると
   1〜5 件目と比べられなくなる」

**変えないもの**: 運用規則・質問票の設問・記録テンプレート・
「1 件」と数える条件・経営ゲートの件数（10 件）

### 手順

1. 上の 3 点を編集する
2. 読み合わせ: 台帳の行が **1〜10 の各 1 回・欠番なし**であること
   （番号は区切りを含めた形で数える。環境メモ 2026-08-09 の項目）。
   README の経営ゲート「制作者インタビュー 10 件」と件数が一致すること
3. git diff に**行の削除が無い**こと（表の分割で見出し行は増えるが、
   データ行は 10 行のまま）
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 区切りを入れたことで「5 件で終わってよい」と誤読される恐れ。
  文言は「5 件で**一区切り**」「反映してから 6 件目へ」とし、
  **目標は 10 件のまま**であることが読めるようにする
- **運用**: 設問を途中で変えると比較できなくなるため、v2 は第 1 巡の後と
  明記する（事例 18 のテスト→修正→再テストの順序）
- **財務/法務**: なし（謝礼・個人情報の扱いは既存の運用規則のまま）
- **対外発信**: なし。実施・連絡・依頼文の送信は人
- **整合**: README の経営ゲート「制作者インタビュー 10 件」・KPI・
  ROADMAP_90D と矛盾しない（数え方も件数も変えない）

## 2026-08-09 12:25 JST 検証手順の罠を環境メモに追記 — 状態: 実装済み 317cc9d

提案: proposals.md 2026-08-09 12:15（⑤の指示 11:50、事例 17）。
恒常規則に従い**既存ファイルの磨き込みのみ**。規則ではなく事実の記録。

### 変更対象ファイル

1. `docs/autonomous-loop.md` — 「環境メモ（プロジェクト固有）」の
   箇条書きの**末尾**に 1 項目追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（追加する 1 項目。既存 3 項目の後ろに置く）

現在の環境メモは 3 項目（docs のみのリポジトリ／他リポジトリは匿名 clone で
読む／予約の :30 は発火がずれる）。日付つきの事実として同じ形で書く:

```
- 2026-08-09: 番号を grep で数えると**部分一致する**（"15" が "5" に
  一致）。事例の索引など番号つき一覧を照合するときは、区切りを含めた形
  （`^## 5\. ` や `・5（`）で数える。第 12 周に④が踏んだ
```

**変えないもの**: 既存 3 項目の文言・順序、節の見出しと前書き
（「このリポジトリで実際に踏んだものだけ書く」以下）

### 手順

1. 環境メモの箇条書きの末尾に上の 1 項目を追加する
2. 読み合わせ: 日付・内容が cycle-report 第 12 周の記述と一致すること。
   **他プロジェクト（site の clamd・pgrep・Playwright 等）の事実を
   持ち込んでいない**こと
3. git diff が**追加のみ**（削除 0）であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用（再現性）**: 環境メモは「実際に踏んだものだけ」が原則。今回は
  第 12 周に④が実際に踏んだ事実なので適合する。**推測や一般論
  （「grep は正規表現に注意」等）に薄めない**こと。踏んだ具体例を残す
- **運用**: 環境メモが増えすぎると読まれなくなる。1 周 1 項目までとし、
  同じ趣旨の項目が既にある場合は追加せず既存を直す（今回は該当なし）
- **財務/法務/対外発信**: なし（内部の作業メモ）
- **整合**: README・STRATEGY・ROADMAP_90D・KPI に触れない。恒常規則の
  表にも触れない（規則の追加ではないため⑤の裁定は不要。⑤自身が
  mediation.md 11:50 でそう指示している）

## 2026-08-09 11:25 JST 事例データベースに領域別の索引を作る — 状態: 実装済み a0a482d

提案: proposals.md 2026-08-09 11:15（事例 11）。恒常規則に従い
**既存ファイルの磨き込みのみ**。本文（各事例の節）は 1 文字も変えない。

### 変更対象ファイル

1. `docs/research/case-studies.md` — 冒頭の「書式:」ブロックの**直前**に
   索引の節を挿入
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（③はこの割り当てをそのまま使う。16 件すべてを 1 回だけ載せる）

見出しは「## 索引（領域別）」。直後に運用の 1 行:
「**事例を足したら、この索引にも 1 行足す**（①の作業に含める）」。

| 領域 | 事例番号 |
| --- | --- |
| 広報・対外表示 | 1（presskit()）・2（Press Kitty）・13（FTC の開示指針）・15（日本のステマ規制） |
| 経理・財務 | 3（Buffer）・4（Ghost）・10（Open Collective）・12（default alive/dead） |
| 経営企画・運用の規律 | 9（GitHub 公開ロードマップ）・11（GitLab handbook）・14（launch checklist）・16（インシデント管理） |
| 計測 | 7（Plausible）・8（GoatCounter） |
| 利用者に聞く | 5（Postmortem の定型）・6（The Mom Test） |

**割り当ての根拠**: 各事例の「学び」が向いている先で分ける。迷うものは
1 か所だけに置く（重複させない＝数え間違いを防ぐ）。

### 手順

1. 上の節を「書式:」の直前に挿入する（既存の説明文は残す）
2. 読み合わせ: 索引に載る番号が **1〜16 の各 1 回・重複なし・欠番なし**で
   あること（機械的に数える）。各事例の本文が無変更であること
3. git diff が**追加のみ**（削除 0）であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用（再現性）**: 索引と本文がずれるのが最大リスク。③は番号の
  重複・欠番を数えてから commit する。以後①が事例を足すときに索引を
  更新し忘れると同じずれが起きるので、**索引の直下に運用の 1 行**を置いて
  忘れにくくする
- **運用**: 領域名は 5 つに固定し、増やさない（増やすと分類の判断が
  毎回発生して 1 周が重くなる）。どこにも当てはまらない事例が出たら
  「経営企画・運用の規律」に置く
- **財務/法務/対外発信**: なし（内部文書の目次）
- **整合**: README・STRATEGY・ROADMAP_90D・KPI に触れない。事例の
  内容・件数・出典は変えない（数え方を変えると 100 件の目標がぶれる）

## 2026-08-09 10:25 JST 第三者に依頼する広報を「要判断」に追加 — 状態: 実装済み 6127d3c

提案: proposals.md 2026-08-09 10:15（事例 13・14・15）。恒常規則に従い
**既存ファイルの磨き込みのみ**。**制限を足すだけで既存行は緩めない。**

### 変更対象ファイル

1. `docs/autonomous-loop.md` — 「人が決めるまで変えない一覧」の表に 1 行追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（追加する 1 行。挿入位置は「対外発信」行の直後）

現在の表は 6 行（経営ゲートの数値／事業の順序と北極星指標／対外発信／
金額・契約・支払い・価格の決定／main への push・PR の merge／
他リポジトリへの push）。「対外発信」の直後に、関連する制限として置く:

| 項目 | 理由 |
| --- | --- |
| **第三者に依頼する広報**（インフルエンサー・レビュアー・記事広告などへの依頼） | 日本のステマ規制では**表示の責任は広告主となる事業者**にあり、依頼した SIDRA 側が負う（消費者庁・2023-10-01 施行。docs/research/case-studies.md の 15）。個別の適法性は人（必要なら専門家）が判断する |

**変えないもの**: 既存 6 行の文言・順序、および表の前後の文
（「判断が必要になったら…要判断と明記して」以降）

### 手順

1. 「対外発信」行の直後に上の 1 行を挿入する
2. 読み合わせ: 行の理由欄が事例 15 の事実（所管・施行日・対象）と一致し、
   **適法性の判断を書いていない**こと（「違法」「問題ない」と書かない）。
   既存 6 行が一字も変わっていないこと
3. git diff が**追加 1 行のみ**であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **法務**: 法令の解釈を社内文書が断定するのが最大リスク。書くのは
  **制度の事実と「人が決める」という線引きだけ**。適法・違法の判定や
  「この書き方なら大丈夫」に類する記述はしない
- **信用**: 制限の追加なので、誤って緩める方向の変更が入らないよう
  ③は diff を確認する（既存行の削除・変更が 0 であること）
- **財務**: なし（金額・契約に触れない）
- **対外発信**: なし。press-kit には法令の記述を**足さない**
  （第 9 周の設計どおり。一次資料は事実のみ）
- **整合**: README・STRATEGY・ROADMAP_90D・KPI に触れない。既存の
  「対外発信は下書きまで」と矛盾せず、その範囲を狭める方向のみ

## 2026-08-09 09:25 JST 8/10 起点日の実態を経営記録に残す — 状態: 実装済み f46e1a3

提案: proposals.md 2026-08-09 09:15（事例 14）。恒常規則に従い
**既存ファイルの磨き込みのみ**。他リポジトリへの push はしない（読むだけ）。

### 変更対象ファイル

1. `docs/finance/monthly-record.md` — 2026-08 節の「4. 特記事項」に追記
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（書く事実 4 点。出典は marketing リポジトリ・2026-08-09 確認）

「4. 特記事項」の末尾に小見出し「2026-08-10 週の広報発信・営業の起点日
（他部門の状況。marketing リポジトリで確認）」を作り、次を箇条書きで書く:

| 書く事実 | 出典 |
| --- | --- |
| 広報文面のドラフトは完成済み。**社長承認は未了** | marketing docs/research/mediation.md 2026-08-09 08:55 |
| site 側の**発信開始ゲート 6 項目は未達** | 同上 |
| 営業の判定表は**証跡付き完了 0/6** | 同上 |
| marketing の⑤は「**承認とゲートの両方が揃うまで発信しない**」と裁定 | 同上 |

末尾に 1 行:「Fg 側の記録であり、判断・催促はしない（発信の可否は
marketing のゲートと社長の承認による）」

**書かないもの**: 評価・原因の断定・責任の所在・見通し（いつ出せるか）・
未確認の数字。marketing の内部作業を Fg 側で指図する表現

### 手順

1. 上の 4 点を「4. 特記事項」の末尾に追記する（既存の特記事項は残す）
2. 読み合わせ: 4 点それぞれが marketing の mediation.md の記述と一致すること
   （転記元の該当行を確認してから書く）。**Fg 側の意見を混ぜない**
3. git diff が**追加のみ**であること
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 他部門の状況を Fg が要約する行為なので、**転記のずれが最大の
  リスク**。出典（リポジトリ名・ファイル・日時）を明記し、評価語を混ぜない。
  「遅れている」「間に合わない」等の判断語を使わない
- **越境**: marketing・site への push はしない。Fg の記録に留める。
  marketing への周知が要る場合も人か gdp が行う（分担 0d4777d）
- **財務/法務**: なし（金額・契約に触れない）
- **対外発信**: なし（内部記録）
- **整合**: README の「第 2 週ユーザー獲得 → 第 3 週スポンサー営業」の
  節目に対する事実記録であり、経営ゲートの数値や事業の順序は変えない
  （「人が決めるまで変えない一覧」に触れない）

## 2026-08-09 08:25 JST プレスキットに「広告・スポンサーの扱い」節を追加 — 状態: 実装済み c2d1f9f

提案: proposals.md 2026-08-09 08:15（事例 13）。恒常規則に従い
**既存ファイルの磨き込みのみ**。書くのは site SPEC で確認済みの事実だけ。

### 変更対象ファイル

1. `docs/pr/press-kit.md` — 「特徴」節と「画像・ロゴ」節の**あいだ**に
   「広告・スポンサーの扱い」節を追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（③はこの 5 点だけを書く。site SPEC の行番号は確認済み）

| 書く事実 | 出典（site SPEC・2026-08-09 確認） |
| --- | --- |
| 収益は広告・アフィリエイトを想定。サイト上での有料販売はしない | §1 |
| **PR 表記なしの協賛を作らない**。スポンサー掲載（PR 帯）は sponsor 必須・期間必須で運営が管理 | §5・§6（`data/sponsors/*.json`） |
| 協賛が絡む内容は必ず特集（PR 表記が型で強制される側）に置く。編集記事（`/articles/`）は sponsor 欄を持たない | §6 |
| ジャムのスポンサーは name 必須（無ければ sponsor ごと捨てる）。賞金・賞品の受け渡しはサイトの外 | §5 |
| **順位は付けない**。フォロワー数・お気に入り人数・ジャムの順位を出さない | §5 |

節の末尾に 1 行: 「掲載料・期間・条件は決まっていない（人が決める）」

**書かないもの**: 料金・掲載単価・在庫数・成約実績、日本の景表法まわりの
解釈（未確認）、site SPEC に無い新しい約束

### 手順

1. 上の 5 点を press-kit の該当位置に追加する（数字は書かない）
2. 読み合わせ: 5 点それぞれが site SPEC の記述と矛盾しないこと。
   press-kit 冒頭の「出典」注記が §5・§6 も含む形になっていること
   （現在は README.md・SPEC.md とあるので追記不要か確認）
3. git diff が**追加のみ**であること（既存の節を書き換えない）
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 「PR 表記なしを作らない」は対外的な約束として読まれる。site の
  実装がそうなっている事実の転記に留め、**強めない・広げない**
  （例:「一切の広告表記を保証」のような書き方をしない）
- **法務**: 日本の景表法まわりは未確認なので触れない（事例 13 の注記どおり）。
  外部の指針（FTC）を自社の約束のように書かない
- **財務**: 料金・条件を書かない。掲載料の決定は人（「人が決めるまで
  変えない一覧」の金額・契約・価格に該当）
- **対外発信**: press-kit は下書きのまま。公開・配信は人
- **整合**: STRATEGY「実績のない段階でスポンサー営業を大量送信しない」に
  反しない（資料の事実整備であって送信ではない）。分担（0d4777d）にも適合
  — Fg が一次資料、marketing がそれを引く

## 2026-08-09 07:25 JST 恒常規則を規則文書へ集約 — 状態: 実装済み 6f24440

提案: proposals.md 2026-08-09 07:15（事例 11）。恒常規則に従い
**既存ファイルの磨き込みのみ**。転記であり、緩和も新設もしない。

### 変更対象ファイル

1. `docs/autonomous-loop.md` — 「回し方の決まり」節の直後に
   「### 恒常規則（⑤の裁定で追加。緩和はしない）」小節を追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（転記元と一字一句の対応を保つ）

挿入位置は「## 5 役ローテーション」内の「### 回し方の決まり」の直後
（回し方の規則の続きとして読める場所）。表 1 つで 3 本を並べる:

| 規則 | 内容 | 解除条件 | 出典 |
| --- | --- | --- | --- |
| 受け皿の新設停止 | 人への未回答質問が 4 件以上のあいだは新規文書の型を作らず、事例累積と既存の磨き込みに切り替える | 未回答が 3 件以下になったら解除 | mediation.md 2026-08-08 22:50 |
| 変化なし周のスキップ | 新しい事実（gdp・人の回答・他リポジトリの変化）が無い周は確認だけで終了してよい | なし（常時） | mediation.md 2026-08-09 00:50 |
| 1 周を軽くする | ①は事例 1〜2 件・提案 1 件・裏取り URL 2 件までに抑える。②③は既存ファイルの磨き込みを優先 | なし（常時） | mediation.md 2026-08-09 06:32 |

表の直後に 2 行:
- **この表は⑤の裁定の転記。追加は⑤だけが行い、緩和はしない**
- 現在の発動状況は各周が自分で確かめる（未回答質問の数は
  `docs/cycle-report.md` の最新の「人へ仰ぎたいこと」を数える）

**変えないもの**: 既存の節・文はすべて（追加のみ）

### 手順

1. 上の小節を「### 回し方の決まり」の直後に挿入する
2. 読み合わせ: 3 本の内容が mediation.md の各出典の記述と一致すること。
   git diff が**追加のみ**（削除 0）であること
3. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 転記のずれ（意味が変わる要約）が最大リスク。各行に出典の日時を
  書き、③は転記元と読み合わせてから commit する。**規則を緩める方向の
  書き換えはしない**（緩和は⑤の裁定を経る）
- **運用**: 規則が 2 か所（mediation.md と autonomous-loop.md）に載るため、
  今後の追加は⑤が裁定→①〜③が規則文書へ反映、という既存の経路
  （0d4777d で確立）に一本化する。この点も表の直後の 2 行で明示する
- **法務/財務**: なし（ループ運用の記述のみ）
- **対外発信**: なし
- **整合**: README・STRATEGY・ROADMAP_90D・KPI に触れない。恒常規則
  （受け皿新設停止）自体にも適合（新規ファイルを作らない）

## 2026-08-09 02:25 JST 分担の規則反映 — 状態: 実装済み 0d4777d

提案: proposals.md 2026-08-09 02:15（事例 11・⑤裁定 01:50）。
恒常規則に従い**既存ファイルの磨き込みのみ**。

### 変更対象ファイル

1. `docs/autonomous-loop.md` — 「このリポジトリの担当」節に分担の小節を追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成

「このリポジトリの担当」節の既存リスト（参照リポジトリ 3 つと
「参照は読み取りのみ」の段落）の直後に、次の小節を 1 つ追加する:

```
### marketing との分担（⑤の裁定 2026-08-09 01:50）

| | 担当 |
| --- | --- |
| **Fg（ここ）** | 全社戦略・経理・経営企画・プレスキット（会社とプロダクトの事実の一次資料）・月次経営レコード・インタビューの型 |
| **marketing** | 営業資料・広報文面・掲載結果レポート（発信・商談の実行資料） |

- marketing が GAMEYARD の事実を使うときは Fg の docs/pr/press-kit.md を
  一次資料として参照する（同じ事実の食い違いを防ぐ）
- どちらのループも相手リポジトリへ push しない。marketing 側への周知は
  人か gdp が marketing の Issue 経由で行う
```

**変えないもの**: 同節のそれ以外の文・他の節すべて（裁定の転記であり、
新しい規則をここで発明しない）

### 手順

1. 上の小節を追加する（mediation.md 01:50 の裁定から転記。文言を
   足さない・削らない方向で整える）
2. 読み合わせ: mediation.md の裁定と内容が一致していること。
   git diff が追加のみ（既存行の削除・変更なし）であること
3. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 裁定と規則文書の食い違いが最大リスク。転記元（mediation.md
  01:50）を明記し、内容を足さない・削らない
- **法務/財務**: なし（分担の記述のみ。金額・契約に触れない）
- **対外発信**: なし
- **整合**: README「文書」一覧・STRATEGY と矛盾しない（marketing の存在は
  既に「このリポジトリの担当」節が参照している）。marketing 側の規則には
  触れない（越境しない）

## 2026-08-09 01:25 JST プレスキットに公開 URL を反映 — 状態: 実装済み f77033b

提案: proposals.md 2026-08-09 01:15。恒常規則に従い**既存ファイルの
磨き込みのみ**（新規ファイルなし）。

### 変更対象ファイル

1. `docs/pr/press-kit.md` — 事実シートの URL 行と概要の 1 行
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（変更は 3 点だけ）

1. 事実シート「公開 URL」行: 「準備中」→ `https://play-game-yard.com`
2. 概要の末尾に 1 行: 「公開 URL: https://play-game-yard.com」
3. 冒頭の出典注記に追記: 「公開 URL は marketing リポジトリ README と
   実サイトの稼働確認（2026-08-09）による」
   — このファイルの「事実の出典を明記する」規律を URL にも適用する

**変えないもの**: 連絡先（人が決める）・画像/ロゴ（未整備のまま）・
数字の節（変更理由なし）

### 手順

1. 上の 3 点を編集する
2. 読み合わせ: URL 表記が 2 か所とも一致すること。ほかの節に意図しない
   差分が無いこと（git diff で行数を確認）
3. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: URL の typo が最大のリスク（間違った URL を配れば実害）。
  marketing README からのコピーと実サイト確認済みの文字列
  `https://play-game-yard.com` を機械的に使い、手打ちしない
- **法務**: なし（自社サイトの URL）
- **財務**: なし
- **対外発信**: なし（プレスキットは引き続き下書き。公開・配信は人）
- **整合**: site の README/SPEC には URL 記載が無いままなので、出典は
  marketing README と実サイト確認であることを明記（出典の偽装をしない）

## 2026-08-09 00:25 JST A 指標の集計手順書 — 状態: 実装済み f20e2cc

提案: proposals.md 2026-08-09 00:15（事例 3・10）。恒常規則に従い
**新規ファイルなし**（monthly-record への追記と列追加のみ）。

### 変更対象ファイル

1. `docs/finance/monthly-record.md` — 「A 指標の集計手順」節の追記＋収支表に
   「根拠の所在」列を追加
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（手順書に固定する定義）

- **入力**: 本番の投稿レコードの export。**依頼する列は `slug` と `createdAt`
  の 2 つだけ**（作者ハンドル・メール等の個人データを持ち込まない）。
  `createdAt` は ISO 8601 UTC（site `server/store.test.mjs` の形式
  `2026-01-02T00:00:00.000Z` で確認済み）
- **週の区切り**: JST に変換して**月曜 00:00 始まり**（日本語圏運用と一致）。
  週の表記は「その週の月曜の日付」（例: 2026-08-03 週）
- **数え方**: `createdAt` がその週に入るレコードの件数。公開/非公開を
  区別できる列が export に無い場合は**総数として数え、その旨を備考に書く**
  （数字の種別を明記する規律は press-kit と同じ）
- **記録の書き方**: KPI 表の「今月実績」に週ごとの件数（例:
  `w08-03: 2 / w08-10: 5`）、備考に export の取得日を書く
- **収支表の列追加**: 「根拠の所在」列（領収書・明細の**保管場所だけ**を
  人が書く。中身・金額の転記はしない）

### 手順

1. monthly-record の 2026-08 節の下に「A 指標の集計手順」節を追記する
   （上の定義を過不足なく。実数は書かない）
2. 収支表のヘッダに「根拠の所在」列を足し、既存 3 行は「（人の入力待ち）」
3. 読み合わせ: KPI 指標名・目標値が不変であること。手順だけ読んで第三者が
   同じ数字を出せるか（定義に曖昧語が残っていないか）
4. commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **法務/プライバシー**: export の依頼列を slug と createdAt に限定し、
  個人データをこのリポジトリに持ち込まない（git 履歴は消せない）
- **信用**: 週の区切り・数え方を先に固定し、後から数字に合わせて定義を
  動かせないようにする（定義変更は人の判断として記録する）
- **財務**: 「根拠の所在」列は保管場所のみ。金額の記入・決定はすべて人
- **対外発信**: なし
- **整合**: KPI.md の指標名・目標値は不変。README 経営ゲートとの対応も不変。
  恒常規則（受け皿新設停止）に適合

## 2026-08-08 23:25 JST KPI 計測手段の棚卸し — 状態: 実装済み a1a5bc9

提案: proposals.md 2026-08-08 23:15（事例 7・8）。恒常規則（22:50）に従い
**新規ファイルなし**。site は読むだけで変更しない。

### 変更対象ファイル

1. `docs/finance/monthly-record.md` — 2026-08 節の「計測手段」列を事実ベースに更新
2. `docs/research/designs.md` / `proposals.md` — 印と状態（役割③）
3. `docs/worklog.md` — 3 行以内の記録

### 構成（分類の定義）

- **A（集計手順が書ける）**: site 等のリポジトリ上の**保存の形に時刻つき記録が
  確認できる**指標。根拠ファイルのパスを計測手段列に書く。
  ※リポジトリから分かるのは「何が記録される設計か」まで。**本番にデータが
  実在するかは、このループからは確認できない**と特記事項に明記する（誇張防止）
- **B（site 側の実装が要る）**: 記録自体が設計に無い指標（アクセス系:
  週次アクティブプレイヤー・プレイ開始成功率・7 日再訪率 など）。
  「実装するなら個人追跡なしの選択肢が実在する（事例 7・8）」と注記する
- 実数は書かない（本番データに触れないため。数字が書けるのは人が export
  した後）

### 手順（③が読む場所・実在確認済み）

1. site の `SPEC.md` §5（感想・評価）・§8 と、`server/lib/`・
   `server/store.test.mjs`・`server/api.mjs` を読み、投稿・感想・評価
   レコードに時刻があるかを確認する
2. creater-yard を read clone し、「CreatorYard 掲載制作者」の数え方の根拠に
   なるデータの形を確認する（clone が 10 分に収まらなければ background に
   回し、次の役割は見送りと worklog に書く）
3. monthly-record の計測手段列を A / B で更新する（KPI の指標名・目標値は
   一字も変えない）
4. 読み合わせのうえ commit 1 件＋印の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 「A」と書けるのは保存の形が確認できた指標だけ。本番データの
  実在・件数には言及しない。分類に迷う指標は B に倒す（楽観に倒さない）
- **法務**: 読むのはデータの**形**だけ。個人データの中身・検査や認証の実装
  詳細をこのリポジトリへ転記しない
- **財務**: 支出なし。計測ツール導入の判断はしない（B の注記に留める＝
  導入判断は site 側・人）
- **対外発信**: なし
- **整合**: KPI.md の指標名・目標値を変えない。site・creater-yard への変更なし
  （read only）。恒常規則（受け皿新設停止）に適合

## 2026-08-08 22:25 JST 制作者インタビューの型 — 状態: 実装済み 306dd21

提案: proposals.md 2026-08-08 22:15「制作者インタビューの型」
（根拠事例: case-studies.md の 5・6）。型と受け皿のみ。実施は人。

### 変更対象ファイル

1. `docs/research/interviews.md`（新規）— 質問票＋記録テンプレ＋台帳
2. `docs/research/designs.md` — 実装済み印（役割③）
3. `docs/research/proposals.md` — 状態を「実装済み」へ（役割③）
4. `docs/worklog.md` — 3 行以内の記録

### 構成

- **運用規則**: 実施・連絡・依頼文の送信は人 / 記録は事実のみ /
  相手はハンドル名のみ（本人が公開している範囲）/ 引用は同意の範囲だけ /
  **「1 件」と数える条件 = 質問票を完走し記録が完備していること**
  （数合わせの薄い聞き取りを防ぐ）
- **質問票 v1**（The Mom Test 準拠。意見でなく過去の行動の事実を聞く）:
  1. 最近ゲームを公開したのはいつ・どこでしたか（過去の行動）
  2. その公開先はどうやって決めましたか
  3. 公開までで一番時間を取られたことは何でしたか
  4. GAMEYARD を試した人には: どこで詰まりましたか。
     試していない人には: **今使っている場所で困っていること**を聞く
     （「何があれば使いますか」は意見・未来の話なので聞かない）
  5. 公開後、感想や数字をどう見ていますか
  - 締めに Postmortem 形式: **良かった点 3 つ / 詰まった点 3 つ**（同数）
- **記録テンプレ**（1 件 1 節）: 日時 / 相手（ハンドル）/ 経路 /
  各質問への回答 / 引用同意の範囲 / 学び 1 行
- **台帳**: 1〜10 の表（実施日・相手・状態）。経営ゲート進捗が一目で分かる

### 手順

1. 上の構成で `docs/research/interviews.md` を作る（台帳は全行「未実施」）
2. 読み合わせ: README の経営ゲート「制作者インタビュー 10 件」と件数・
   定義が対応していること。依頼文の送信を促す文言を入れないこと
3. commit 1 件＋状態更新の commit

### リスク（財務・法務・信用/ブランド）

- **法務/プライバシー**: 個人が特定される情報（本名・所属・連絡先）を
  書かない。git 履歴は消せないため、引用は本人が同意した範囲のみ
- **信用**: 誘導質問で「良い声」を作らないこと自体が型の目的（The Mom Test）。
  「1 件」の定義を固定し、数合わせを防ぐ
- **財務**: 支出なし。謝礼の有無・金額を決めない（決めるなら人）
- **対外発信**: 依頼文の送信・実施は人。質問票はその台本にすぎない
- **整合**: README ゲート条件「制作者インタビュー 10 件」に直結。
  ROADMAP_90D の「制作者インタビュー」記述・KPI.md と矛盾なし

## 2026-08-08 21:25 JST 月次経営レコードの受け皿 — 状態: 実装済み 530cdc4

提案: proposals.md 2026-08-08 21:20「月次経営レコードの受け皿」
（根拠事例: case-studies.md の 3・4）。記録の器を作る。金額の決定はしない。

### 変更対象ファイル

1. `docs/finance/monthly-record.md`（新規）— 台帳本体＋2026-08 の初回レコード
2. `docs/research/designs.md` — 実装済み印（役割③）
3. `docs/research/proposals.md` — 状態を「実装済み」へ（役割③）
4. `docs/worklog.md` — 3 行以内の記録

### 構成

冒頭に運用規則、以降は月ごとに 1 節を追記（新しい月を上に）。

- **運用規則**: 事実だけ書く / ゼロはゼロと記録 / 未計測は「未計測」と書く /
  金額・価格の決定はここでしない / 公開・共有の判断は人
- **月次節のテンプレート**（毎月同じ形。Buffer の月次定型レポートに倣う）:
  1. 収支 — 収入・支出・差引。**実額は人にしか分からないため、AI が書けるのは
     「人の入力待ち」の明示まで**。人が記入したら以降その行は人の領域
  2. KPI 実績 — docs/KPI.md の KPI ツリーと**同じ 9 指標・同じ並び・同じ
     初期目標**の表に「今月実績」「計測手段」列を足す。実績が取れない指標は
     「未計測（計測手段なし）」と書き、site 側に実装が要ることを明示
  3. 経営ゲート判定 — README の経営ゲート 6 条件に対する充足状況
  4. 特記事項 — 3 行以内

### 手順

1. 上の構成で `docs/finance/monthly-record.md` を書き、2026-08 の初回
   レコードを「入力待ち / 未計測」の明示つきで作る
2. 読み合わせ: KPI.md と指標名・初期目標が一字一句一致すること。
   README の経営ゲート 6 条件と対応が取れていること
3. commit 1 件＋状態更新の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 数字の捏造が最大リスク。収支実額は「人の入力待ち」と明示し、
  KPI 実績は計測できるまで「未計測」と書く。それらしい数字を絶対に埋めない
- **法務**: 取引先名・個人の報酬など秘匿情報は書かない。git 履歴は消せない
  ため、実額・固有名詞を載せる判断は公開範囲を人が決めてからにする
- **財務**: 記録のみで金額の決定はしない。KPI.md「収益のガードレール」に
  触れる変更もしない
- **整合**: KPI.md の指標・目標値をそのまま使う（別の数字を発明しない）。
  README 経営ゲートの 6 条件と対応させる。ROADMAP_90D の週次構成とは
  粒度が違う（月次）だけで矛盾しない
- **対外発信なし**: この台帳は内部文書。公開は人が決める

## 2026-08-08 20:25 JST GAMEYARD プレスキットの下書き — 状態: 実装済み 483c25b

提案: proposals.md 2026-08-08 20:15「GAMEYARD プレスキットの下書き一式」
（根拠事例: case-studies.md の 1・2）。成果物は下書き文書のみ。対外公開・
配信はしない。

### 変更対象ファイル

1. `docs/pr/press-kit.md`（新規）— 下書き本体
2. `docs/research/designs.md` — 実装済み印（役割③が付ける）
3. `docs/research/proposals.md` — 状態を「実装済み」へ（役割③）
4. `docs/worklog.md`（無ければ作成）— 3 行以内の記録

### 構成（presskit() の標準構造に合わせ、節ごとに事実の出典を固定する）

| 節 | 事実の出典 |
| --- | --- |
| 概要（GAMEYARD とは） | site の README 冒頭（投稿プラットフォーム＋収集カタログ、SEO/AIO 最適化） |
| 事実シート（開発元・形態・URL・価格） | 開発元: SIDRA STUDIO。公開 URL が文書で確認できなければ「準備中」と書く（作らない） |
| 数字 | site README「実際に収集したデータ」の表から転記（例: 収集 21,907 件・生成 25,581 ページ・ブラウザ直接プレイ 3,916 件）。**転記元と確認日を明記。丸めない** |
| 特徴 | site SPEC §1（何か）・§3（投稿と 8 段階の検査） |
| 画像・ロゴ | 現状「未整備」と明記。置き場所だけ `docs/pr/assets/` に決める。**画像の捏造・生成はしない** |
| 連絡先 | 公開連絡先が文書に無ければ「（人が決める）」と空欄で残す |

### 手順

1. site の README.md・SPEC.md §1〜§3 を読み、事実を転記する
2. 上の構成で `docs/pr/press-kit.md` を書く。推測・未確認の数字を書かない
3. 読み合わせ: Fg の README・STRATEGY・ROADMAP_90D・KPI と矛盾しないか。
   特に**経営ゲート未達の段階でユーザー実績風の表現を書かない**
4. commit 1 件（press-kit 本体）＋状態更新の commit

### リスク（財務・法務・信用/ブランド）

- **信用**: 実績の誇張が最大のリスク。「収集カタログの件数」と「投稿作品の
  実績」（まだ経営ゲート未達）を混同させない。数字には必ず「収集データ」
  「事前生成ページ」と種別を付ける
- **法務**: 収集件数は自社集計として出典明記。Steam / itch.io の名称は
  事実の記述（収集元）に留め、提携があるかのような表現をしない
- **財務**: 支出なし。価格・スポンサー料金は書かない（金額の決定は人）
- **対外発信**: このファイルは下書き。公開・配信・送付は人が決める
  （「人が決めるまで変えない一覧」の対象）
- 整合: ユーザー獲得優先（8 月第 2 週）→ 第 3 週スポンサー営業という
  README の順序に対し、営業開始前の資料準備は矛盾しない
